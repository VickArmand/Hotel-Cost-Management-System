<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hotelcostmanagementsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelCostManagementSystem">
        <activity
            android:name=".CashierHome"
            android:exported="false"
            android:label="@string/title_activity_cashier_home"
            android:theme="@style/Theme.HotelCostManagementSystem.NoActionBar" />
        <activity
            android:name=".AdminHome"
            android:exported="false"
            android:label="AdminHome"
            android:theme="@style/Theme.HotelCostManagementSystem.NoActionBar" />
        <activity
            android:name=".CashierMainActivity"
            android:exported="false" />
        <activity
            android:name=".AdminMainActivity"
            android:exported="false" />


        <activity
            android:name=".Cashiers"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".CashierMain" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_cashier_main" -->
        <!-- android:theme="@style/Theme.HotelCostManagementSystem.NoActionBar" /> -->
        <!-- <activity -->
        <!-- android:name=".CashierMainActivity" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_cashier_main" -->
        <!-- android:theme="@style/Theme.HotelCostManagementSystem.NoActionBar" /> -->
        <!-- <activity -->
        <!-- android:name=".CashierMainAcivity" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_cashier_main_acivity" -->
        <!-- android:theme="@style/Theme.HotelCostManagementSystem.NoActionBar" /> -->

        <activity
            android:name=".Registration"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>